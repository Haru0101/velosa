{
  "devDependencies": {
    "push-dir": "^0.4.1",
    "vuepress": "^1.4.1"
  },
  "scripts": {
    "dev": "vuepress dev",
    "build": "vuepress build",
    "eject": "vuepress eject",
    "deploy": "push-dir --dir=./.vuepress/dist --branch=gh-pages --cleanup"
  }
}