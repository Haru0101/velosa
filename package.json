{
  "devDependencies": {
    "push-dir": "^0.4.1",
    "vuepress": "^1.4.1"
  },
  "scripts": {
    "dev": "vuepress dev src",
    "build": "vuepress build src",
    "eject": "vuepress eject src",
    "deploy": "push-dir --dir=./src --branch=gh-pages --cleanup"
  }
}